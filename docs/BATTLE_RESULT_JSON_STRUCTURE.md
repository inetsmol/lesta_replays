# Структура battle_result.json

## Обзор

Файл `battle_result.json` содержит полные данные о результатах боя из World of Tanks. Это двухуровневый массив JSON:

```
[
  METADATA,                    // [0] - Метаданные реплея
  [                            // [1] - Массив с тремя элементами
    BATTLE_RESULTS,            //   [0] - Детальные результаты боя
    EXTENDED_VEHICLE_INFO,     //   [1] - Расширенная инфо об участниках (дубликат data[0].vehicles)
    FRAGS                      //   [2] - Количество убийств (дубликат vehicles[].kills)
  ]
]
```

**Важно:** Основная информация находится в `data[0]` и `data[1][0]`. Элементы `data[1][1]` и `data[1][2]` содержат избыточные данные (дубликаты).

---

## 1️⃣ METADATA (data[0]) - Метаданные реплея

Содержит базовую информацию о реплее и общие данные боя.

### Основные поля:

| Поле | Тип | Описание | Пример |
|------|-----|----------|--------|
| `arenaUniqueID` | int | Уникальный ID арены | `108372291604125656` |
| `battleType` | int | Тип боя (1 = Random, 19 = Ranked, и т.д.) | `1` |
| `clientVersionFromExe` | str | Версия клиента из .exe | `"1.36.0.0"` |
| `clientVersionFromXml` | str | Версия клиента из XML (с локализацией) | `"Мир танков v.1.36.0.0 #1991"` |
| `dateTime` | str | Дата и время боя | `"03.08.2025 11:03:43"` |
| `gameplayID` | str | Режим игры | `"assault"`, `"domination"`, `"ctf"` |
| `hasMods` | bool | Наличие модификаций | `true/false` |
| `mapDisplayName` | str | Отображаемое название карты | `"Карелия"` |
| `mapName` | str | Техническое название карты | `"01_karelia"` |
| `playerID` | int | ID игрока-владельца реплея | `6478029` |
| `playerName` | str | Имя игрока-владельца | `"300KAPTABLLEB"` |
| `playerVehicle` | str | Техника игрока | `"usa-A163_H_3"` |
| `regionCode` | str | Регион сервера | `"RU"`, `"EU"`, `"NA"` |
| `serverName` | str | Название сервера | `"RU6"` |

### Вложенные объекты:

#### `serverSettings` (dict, 8 элементов)
Конфигурация сервера для различных игровых режимов:

- `spgRedesignFeatures` - настройки САУ
- `ranked_config` - конфигурация ранговых боёв (52 параметра)
- `comp7_config` - настройки режима 7x7
- `battle_royale_config` - настройки королевской битвы (19 параметров)
- `fun_random_config` - настройки веселого рандома
- `story_mode_settings` - настройки сюжетного режима
- `epic_config` - настройки эпического режима (27 параметров)
- `vehicle_post_progression_config` - пост-прогрессия техники

#### `vehicles` (dict, 30 записей)
Базовая информация о всех участниках боя. Ключ - `avatarSessionID` игрока.

**Структура каждой записи:**

| Поле | Тип | Описание |
|------|-----|----------|
| `avatarSessionID` | str | ID сессии игрока |
| `vehicleType` | str | Тип техники, формат: `"nation:tank_code"` |
| `name` | str | Реальное имя игрока |
| `fakeName` | str | Анонимизированное имя (если включено) |
| `clanAbbrev` | str | Тег клана |
| `team` | int | Команда (1 или 2) |
| `maxHealth` | int | Максимальное здоровье техники |
| `isAlive` | int | Жив ли игрок (0/1) |
| `isTeamKiller` | int | Тимкиллер ли игрок (0/1) |
| `ranked` | list | Ранговые показатели `[division, rank]` |
| `badges` | list | Значки игрока `[[badge_ids], []]` |
| `wtr` | int | WTR рейтинг |
| `igrType` | int | Тип IGR аккаунта |
| `forbidInBattleInvitations` | bool | Запрет приглашений в бой |
| `botDisplayStatus` | int | Статус бота |
| `customRoleSlotTypeId` | int | ID кастомной роли (0-4) |
| `overriddenBadge` | int | Переопределённый значок |
| `vehPostProgression` | list | ID модулей пост-прогрессии техники |
| `personalMissionIDs` | list | ID активных личных миссий |
| `personalMissionInfo` | dict | Прогресс личных миссий `{mission_id: [status, progress]}` |
| `events` | dict | Игровые события |

**⚠️ Важная особенность полей `name` и `fakeName`:**

Значения зависят от настройки анонимизации в игре:

**БЕЗ анонимизации** (`name == fakeName`):

- `name` = настоящее имя игрока (имя в бою)
- `fakeName` = настоящее имя игрока (дублируется)

**С анонимизацией** (`name != fakeName`):

- `name` = НАСТОЯЩЕЕ имя игрока
- `fakeName` = АНОНИМНОЕ имя (показывалось в бою)

Это соответствует полям в `data[1][0].players`:

- `vehicles.name` ⟷ `players.realName` (настоящее имя)
- `vehicles.fakeName` ⟷ `players.name` (имя в бою)

См. подробное объяснение в разделе [2.3 PLAYERS](#23--players---данные-всех-игроков-30-записей).

---

## 2️⃣ BATTLE_RESULTS (data[1][0]) - Результаты боя

Детальная статистика боя, разбитая на 5 основных секций.

### 2.1 📊 COMMON - Общие данные боя

Информация о самом бое (длительность, результат, тип арены и т.д.)

| Поле | Тип | Описание |
|------|-----|----------|
| `arenaCreateTime` | int | Unix timestamp создания арены |
| `arenaTypeID` | int | ID типа арены |
| `bonusType` | int | Тип боя (см. ARENA_BONUS_TYPE) |
| `duration` | int | Длительность боя в секундах |
| `winnerTeam` | int | Команда победителя (0=ничья, 1/2=команда) |
| `finishReason` | int | Причина завершения боя |
| `finishAllPlayersLeft` | bool | Все игроки покинули бой |
| `guiType` | int | Тип интерфейса |
| `vehLockMode` | int | Режим блокировки техники |
| `gasAttackWinnerTeam` | int | Победитель газовой атаки |
| `division` | int/None | Дивизион (для ранковых) |
| `teamHealth` | dict | Оставшееся здоровье команд `{'1': hp, '2': hp}` |
| `bots` | dict | Информация о ботах `{accountDBID: [team, 'bot_technical_name']}` |
| `commonNumCaptured` | int | Общее количество захватов |
| `commonNumDefended` | int | Общее количество защит |
| `commonNumDestroyed` | int | Общее количество уничтожений |
| `commonNumStarted` | int | Общее количество начатых действий |
| `accountCompDescr` | None | Дескриптор аккаунта |

**⚠️ Особенность поля `bots`:**

Содержит словарь ботов в формате: `{accountDBID: [team, bot_technical_name]}`

Пример:
```python
"bots": {
    "46118410": [1, "BotCrew_6_126_204"],
    "46118411": [2, "BotCrew_6_28_186"],
    ...
}
```

**Связь с другими секциями:**

- `accountDBID` из `bots` совпадает с `avatarSessionID` в `vehicles` и `metadata.vehicles`
- `bot_technical_name` совпадает с `fakeName` в `metadata.vehicles`
- У ботов в `metadata.vehicles`: `name` = "отображаемое имя" (например, `:Seiichi Onodera:`), `fakeName` = технический ID (например, `BotCrew_6_126_204`)

**ВАЖНО:** Боты присутствуют только в:
- `common['bots']` - список ботов с командами
- `metadata.vehicles` - базовая информация
- `vehicles` - полная статистика

Боты **ОТСУТСТВУЮТ** в `players` и `avatars`!

### 2.2 👤 PERSONAL - Личные данные владельца реплея

Содержит два ключа:
- **Числовой ключ** (напр. `"7936289"`) - ID типа техники (typeCompDescr)
- **`avatar`** - детальная статистика владельца реплея (83+ поля)

#### Основные поля `avatar`:

| Категория | Поля |
|-----------|------|
| **Идентификация** | `accountDBID`, `clanDBID`, `badges` |
| **Опыт и очки** | `xp`, `achievementXP`, `achievementFreeXP`, `freeXP` |
| **Валюта** | `credits`, `gold`, `crystal`, `eventCoin` |
| **Урон** | `avatarDamageDealt`, `avatarDamaged`, `avatarKills` |
| **Battle Pass** | `battlePassPoints`, `bpChapter`, `bpTopPoints`, `battlePassComplete` |
| **Достижения** | `achievements`, `inBattleAchievements`, `ribbons` |
| **Прогресс** | `PMProgress` (личные миссии), `dogTags` (жетоны) |
| **Прочее** | `markOfMastery`, `prevMarkOfMastery`, `dailyXPFactor10` |

**Важно:** Многие поля содержат суффикс `Replay` (например, `creditsReplay`, `xpReplay`) - это значения, записанные в момент боя для воспроизведения.

### 2.3 👥 PLAYERS - Данные всех игроков (30 записей)

Словарь с информацией об игроках. Ключ - `accountDBID` игрока.

**Структура каждой записи:**

| Поле | Тип | Описание |
|------|-----|----------|
| `name` | str | Имя, отображавшееся в бою (анонимное, если анонимизация включена) |
| `realName` | str | Настоящее имя игрока (или текущее имя аккаунта*) |
| `clanAbbrev` | str | Тег клана |
| `clanDBID` | int | ID клана в базе данных |
| `team` | int | Номер команды (1 или 2) |
| `igrType` | int | Тип IGR аккаунта |
| `prebattleID` | int | ID предбоевой комнаты |

**⚠️ Важная особенность полей `name` и `realName`:**

Значения этих полей зависят от того, была ли включена анонимизация:

**БЕЗ анонимизации** (`vehicles.name == vehicles.fakeName`):

- `name` = имя в бою
- `realName` = текущее имя аккаунта (может отличаться, если игрок сменил ник после боя)

**С анонимизацией** (`vehicles.name != vehicles.fakeName`):

- `name` = анонимное имя (показывалось в бою)
- `realName` = настоящее имя игрока

**Примеры:**

```python
# Игрок без анонимизации (сменил ник после боя)
players['4210498'] = {
    'name': 'Mr_Park_1',      # имя в бою
    'realName': 'ATAL21rus'   # текущий ник (сменил после боя)
}

# Игрок с анонимизацией
players['6478029'] = {
    'name': 'ALENKO_1981_2015',  # анонимное имя в бою
    'realName': '300KAPTABLLEB'  # настоящее имя
}
```

**Универсальное правило:**

- Имя в бою: `players[id]['name']` или `vehicles[session]['fakeName']`
- Настоящее имя: `players[id]['realName']` или `vehicles[session]['name']`

### 2.4 🎯 AVATARS - Детальная статистика игроков (30 записей)

Словарь со статистикой по каждому игроку. Ключ - `accountDBID` игрока.

**Структура каждой записи (15 полей):**

| Поле | Описание |
|------|----------|
| `avatarDamageDealt` | Урон нанесенный аватаром |
| `avatarDamaged` | Урон полученный аватаром |
| `avatarKills` | Убийства аватаром |
| `availablePoints` | Доступные очки |
| `sumPoints` | Сумма очков |
| `totalDamaged` | Общий урон |
| `badges` | Значки игрока |
| `playerRank` | Ранг игрока |
| `fairplayViolations` | Нарушения честной игры |
| `hasBattlePass` | Наличие Battle Pass |
| `battlePassComplete` | Завершен ли Battle Pass |
| `bpChapter` | Глава Battle Pass |
| `bpTopPoints` | Максимальные очки BP |
| `bpNonChapterPointsDiff` | Разница очков вне глав |
| `bpBonusPoints` | Бонусные очки BP |

### 2.5 🚗 VEHICLES - Детальная статистика техники (30 записей)

Словарь со статистикой каждой машины в бою. Ключ - `avatarSessionID`.

**Каждая запись - СПИСОК из 1 элемента!** `vehicles[session_id][0]` содержит словарь с ~80 полями:

#### Основные категории полей:

**Идентификация:**
- `accountDBID` - ID аккаунта игрока
- `typeCompDescr` - дескриптор типа техники
- `team` - команда (1/2)
- `index` - индекс в команде

**Урон и попадания:**
- `damageDealt` - нанесенный урон
- `damageReceived` - полученный урон
- `damageBlockedByArmor` - заблокировано броней
- `piercings` - пробития
- `piercingEnemyHits` - пробивающих попадания по врагам
- `piercingsReceived` - полученных пробитий
- `directHits` / `directEnemyHits` - прямые попадания
- `directHitsReceived` - полученных прямых попадания
- `explosionHits` / `explosionHitsReceived` - попадания осколочно-фугасными
- `sniperDamageDealt` - урон снайперским огнем

**Помощь:**
- `damageAssistedRadio` - помощь по разведке
- `damageAssistedTrack` - помощь по сбитой гусенице
- `damageAssistedStun` - помощь по оглушению
- `damageAssistedSmoke` - помощь по дыму
- `damageAssistedInspire` - помощь по вдохновению

**Уничтожения:**
- `kills` - убийства
- `tkills` - тимкиллы
- `damaged` - подбито машин
- `damagedHp` - здоровье подбитых машин

**Здоровье:**
- `health` - оставшееся здоровье
- `maxHealth` - максимальное здоровье
- `deathCount` - количество смертей
- `deathReason` - причина смерти (-1 = жив)
- `killerID` - ID убийцы

**Активность:**
- `lifeTime` - время жизни в бою (секунды)
- `mileage` - пройденное расстояние (метры)
- `shots` - выстрелов сделано
- `spotted` - обнаружено врагов

**Захват базы:**
- `capturePoints` - очки захвата
- `droppedCapturePoints` - сброшено очков захвата
- `vehicleNumCaptured` - захвачено баз
- `numDefended` - защищено баз
- `capturingBase` - ID захватываемой базы
- `flagCapture` / `soloFlagCapture` - захват флага
- `flagActions` - действия с флагом `[0,0,0,0]`

**Награды и опыт:**
- `xp` - полученный опыт
- `achievementXP` - опыт за достижения
- `achievementFreeXP` - свободный опыт за достижения
- `achievementCredits` - кредиты за достижения
- `credits` - заработанные кредиты
- `achievements` - список достижений
- `inBattleAchievements` - внутриигровые достижения
- `markOfMastery` - знак мастерства (0-4)

**Оглушение (для САУ):**
- `stunDuration` - длительность оглушения
- `stunNum` - количество оглушений
- `stunned` - оглушенных игроков

**Разрушаемые объекты:**
- `destructiblesHits` - попаданий по объектам
- `destructiblesDamageDealt` - урон объектам
- `destructiblesNumDestroyed` - уничтожено объектов
- `destructibleDeaths` - смерти от объектов

**Прочее:**
- `potentialDamageReceived` - потенциальный полученный урон
- `damageReceivedFromInvisibles` - урон от невидимых
- `equipmentDamageDealt` - урон снаряжением
- `artilleryFortEquipDamageDealt` - урон укреплениям артиллерией
- `winPoints` - очки победы
- `rolloutsCount` - количество откатов
- `resourceAbsorbed` - поглощенных ресурсов
- `entityCaptured` - захваченных сущностей
- `numRecovered` - восстановленных
- `isFirstBlood` - первая кровь
- `isTeamKiller` - тимкиллер
- `stopRespawn` - остановка респавна
- `tdestroyedModules` - уничтожено модулей
- `tdamageDealt` - урон по модулям
- `noDamageDirectHitsReceived` - попаданий без урона
- `directTeamHits` - попаданий по союзникам
- `xpPenalty` - штраф опыта
- `xp/assist` / `xp/attack` / `xp/other` - опыт по категориям
- `damageEventList` - список событий урона (обычно None)
- `avatarDamageEventList` - список событий урона аватара (обычно None)

---

## 3️⃣ EXTENDED_VEHICLE_INFO (data\[1\]\[1\]) - Дубликат информации об участниках

**⚠️ ИЗБЫТОЧНЫЕ ДАННЫЕ:** Почти полностью дублирует `data[0].vehicles` (96.3% совпадений)

Словарь с информацией об участниках боя. Ключ - `avatarSessionID`.

**Структура каждой записи (21 поле):**

Идентична структуре `data[0].vehicles` (см. раздел "1️⃣ METADATA"):

- `avatarSessionID`, `name`, `fakeName`, `vehicleType`
- `team`, `clanAbbrev`, `maxHealth`, `isAlive`
- `badges`, `ranked`, `wtr`, `igrType`
- `vehPostProgression`, `personalMissionIDs`, `personalMissionInfo`
- И другие поля...

**Отличия от data[0].vehicles:**

Единственное различие - тип данных поля `isAlive`:

- В `data[0].vehicles`: `isAlive` = `int` (0 или 1)
- В `data[1][1]`: `isAlive` = `bool` (True или False)

**Рекомендация:** Используйте `data[0].vehicles` вместо этой секции, так как это оригинальный источник данных.

---

## 4️⃣ FRAGS (data\[1\]\[2\]) - Количество убийств

**⚠️ ИЗБЫТОЧНЫЕ ДАННЫЕ:** Дублирует `data[1][0].vehicles[session_id][0].kills`

Словарь с количеством убийств для каждого участника. Ключ - `avatarSessionID`.

**Структура каждой записи:**

```json
{
  "frags": 6  // int - количество фрагов (убийств)
}
```

**Пример:**

```python
# Получить количество убийств игрока
session_id = "26315946"

# Способ 1: через data[1][2] (не рекомендуется)
frags_count = data[1][2][session_id]['frags']  # 6

# Способ 2: через data[1][0].vehicles (рекомендуется)
kills_count = data[1][0]['vehicles'][session_id][0]['kills']  # 6

# Результат одинаковый
assert frags_count == kills_count
```

**Рекомендация:** Используйте `data[1][0].vehicles[session_id][0].kills` для получения количества убийств, так как там доступна полная статистика.

---

## Типы боёв (bonusType / ARENA_BONUS_TYPE)

| Значение | Название | Описание |
|----------|----------|----------|
| 1 | REGULAR | Случайный бой |
| 18 | EPIC_BATTLE | Эпический бой |
| 19 | RANKED | Ранговый бой |
| 29 | COMP7 | Режим 7x7 |
| 31 | BATTLE_ROYALE | Королевская битва |

_Полный список см. в `replays/parser/client_adapter/constants.py`_

---

## Использование в коде

### Загрузка и парсинг

```python
import json

# Загрузка файла
with open('battle_result.json') as f:
    data = json.load(f)

# Разделение на секции
metadata = data[0]
battle_results = data[1][0]

# Доступ к данным
print(f"Карта: {metadata['mapDisplayName']}")
print(f"Длительность: {battle_results['common']['duration']} сек")
print(f"Победитель: команда {battle_results['common']['winnerTeam']}")

# Статистика игрока
player_id = str(metadata['playerID'])
player_stats = battle_results['personal']['avatar']
print(f"Опыт: {player_stats['xp']}")
print(f"Кредиты: {player_stats['credits']}")

# Итерация по игрокам
for account_id, player_data in battle_results['players'].items():
    print(f"{player_data['realName']} [{player_data['clanAbbrev']}]")
```

### Использование с Django парсером

```python
from replays.parser.client_adapter import ReplayDataAdapter

# Создание адаптера из payload реплея
adapter = ReplayDataAdapter(replay.payload)

if adapter.is_valid():
    info = adapter.get_reusable_info()

    # Доступ к данным через типизированные объекты
    print(f"Тип боя: {info.common.arena_bonus_type}")
    print(f"Длительность: {info.common.duration}")
    print(f"Победитель: {info.common.winner_team}")

    # Личные данные
    print(f"ID игрока: {info.personal.avatar.account_id}")
    print(f"Команда: {info.personal.avatar.team}")

    # Все игроки
    for player in info.players.values():
        print(f"{player.real_name} [{player.clan_abbrev}]")
```

---

## Примечания

1. **Структура массива**: Файл содержит 3 основные секции:
   - `data[0]` - метаданные реплея (обязательно используется)
   - `data[1][0]` - детальные результаты боя (обязательно используется)
   - `data[1][1]` - дубликат `data[0].vehicles` (избыточно)
   - `data[1][2]` - дубликат `vehicles[].kills` (избыточно)

2. **Анонимизация и имена**: Поля `name`, `fakeName`, и `realName` имеют контринтуитивное поведение!
   - **В vehicles**: `name` = настоящее имя, `fakeName` = имя в бою (анонимное если включено)
   - **В players**: `name` = имя в бою (анонимное если включено), `realName` = настоящее имя
   - **Смена ника**: `players.realName` может отличаться от `vehicles.name`, если игрок сменил ник после боя
   - См. подробности в разделах про vehicles и players

3. **Вложенность vehicles**: В `battle_results['vehicles']` каждое значение - это СПИСОК из 1 элемента, а не словарь напрямую

   ```python
   # Правильно:
   stats = data[1][0]['vehicles']['26315946'][0]['kills']

   # Неправильно:
   stats = data[1][0]['vehicles']['26315946']['kills']  # TypeError!
   ```

4. **accountDBID vs avatarSessionID**:
   - `accountDBID` - постоянный ID игрока в БД (используется в players, avatars)
   - `avatarSessionID` - временный ID сессии в конкретном бою (используется в vehicles)

5. **Replay поля**: Поля с суффиксом `Replay` (например, `creditsReplay`) - это снимки значений для воспроизведения боя

6. **None значения**: Многие поля могут быть `None` (например, `damageEventList`, `division`, `accountCompDescr`)

7. **Локализация**: Некоторые строки содержат unicode escape-последовательности (например, `\u041c\u0438\u0440` = "Мир")

8. **Избыточность данных**: Не используйте `data[1][1]` и `data[1][2]` - эти секции полностью дублируют данные из других частей файла

9. **Боты в бою**: Если бой содержит ботов:
   - Список ботов находится в `common['bots']` = `{accountDBID: [team, 'bot_technical_name']}`
   - `accountDBID` бота совпадает с `avatarSessionID` в `vehicles` и `metadata.vehicles`
   - Боты **присутствуют** в: `metadata.vehicles`, `vehicles`
   - Боты **ОТСУТСТВУЮТ** в: `players`, `avatars`
   - Для идентификации бота: проверить `avatarSessionID` в `common['bots']` или `metadata.vehicles[session]['fakeName']` начинается с `"BotCrew_"`
   - Пример: всего 14 участников, 2 реальных игрока в `players`, 12 ботов в `common['bots']`

---

## См. также

- [INTEGRATION_GUIDE.md](INTEGRATION_GUIDE.md) - Руководство по работе с client_adapter
- [EXTRACTOR_OPTIMIZATION.md](EXTRACTOR_OPTIMIZATION.md) - Оптимизация парсера
- [replays/parser/client_adapter/constants.py](../replays/parser/client_adapter/constants.py) - Константы и типы боёв
